{
  "name": "trayon-org",
  "version": "1.0.0",
  "description": "Trayon Protocol - Real-Time Data Integrity Infrastructure",
  "private": true,
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "start": "npm start --prefix web",
    "lint": "npm run lint --prefix web"
  }
}
